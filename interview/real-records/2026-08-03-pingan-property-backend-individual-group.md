# 2026-08-03 平安产险后端开发工程师面试复盘

## 1. 基本信息

- 公司：平安产险
- 岗位：0382BM-后端开发工程师-个群
- 面试时间：2026-08-03 15:00
- 面试轮次：技术面，具体轮次待确认
- 结果：尚未确认
- 信息来源：候选人面试前提供的 JD，以及面试后对一个陌生问题的即时回忆；没有完整录音或逐题转写
- 记录边界：只归档已经确认的信息，不根据 JD 或准备过程反推现场还问过哪些问题

## 2. 岗位匹配判断

JD 重点覆盖 Java / Spring、分布式架构、MySQL / SQL Server / MongoDB / Redis、Spring Boot / Spring Cloud / MyBatis、Kafka / RabbitMQ、Linux、系统设计，以及金融、支付、清算结算和高并发高可用经验。

与候选人已有证据匹配较强的部分：

1. 四年 Java 后端和云服务结算系统经验。
2. 订单、流水、结算单、辅账、ERP 入账和核销对账链路。
3. 百万级批处理、订单表分区、N+1、亿级表分页、事务边界和 OOM 排障。
4. 数据库父任务抢占、Redis 跨入口互斥、Spring Batch 分片与停止、明细级 Checkpoint 和失败续跑。

需要保留边界的部分：

1. Spring Cloud、Kafka / RabbitMQ 产品细节、MongoDB、Spark 和 Linux 深度不能因为 JD 出现就包装为成熟生产经验。
2. 结算系统的 ERP 最终一致性、批次本地事务、Redis 锁和任务恢复机制不能直接包装为强一致分布式事务方案。

## 3. 已确认现场问题

### 问题 1：分布式事务中的“二级事务、三级事务”是什么

**面试官原问题回忆**

> 面试官在讨论分布式事务时，提到了候选人记忆中的“二级事务”和“三级事务”。

当前无法确认原词是否准确。结合上下文，可能是“两阶段提交 / 三阶段提交”，但也可能是面试官所在系统的内部术语，不能直接把推断写成已确认事实。

**我的现场回答**

候选人当时对该知识点不熟悉，没有形成可还原的机制回答。

**评价**

1. 这是本场已经明确暴露的新知识缺口。
2. 当前不足不在于少背了一个名词，而在于尚未建立“本地事务、分布式提交协议、业务最终一致性方案”的分层地图。
3. 现有项目中的批次本地事务、Redis 锁、任务状态、重试、幂等和补偿可以作为工程映射，但不能用它们替代 2PC / 3PC 机制本身。

**改进版**

在尚未确认术语时，先澄清问题：

> 我先确认一下，您这里说的二级和三级事务，是指分布式事务中的两阶段提交和三阶段提交吗？这两个协议的准确流程和失败边界我目前掌握得不够稳定，我不先猜实现。我的项目里更多使用单库批次事务、任务状态、幂等、重试和补偿来处理 ERP 链路的最终一致性，它和强一致分布式事务不是一回事。面试后我会把两阶段、三阶段以及 TCC、Saga、事务消息的边界补齐。

**后续任务**

1. 由 `BL-025` 核对术语并完成 2PC / 3PC 最小回答卡。
2. 学习时必须同时回答“本质、流程、阻塞 / 超时边界、替代方案、项目映射”，不只记阶段名称。
3. 将结算系统的本地事务与 ERP 最终一致性作为反例边界，避免把已有项目过度包装。

## 4. 面试前准备中已经完成的验证

1. 自我介绍已从技术栈罗列压缩为“Java 身份、结算职责、生产证据、AI 补充、岗位匹配”。
2. 结算系统已能按“服务对象、输入、公共主链路、输出、本人职责”建立系统全貌。
3. 重新确认职责演进：早期参与 2C 支付和 2B 预装，后期独立负责 2B 商推以及公共流水和核销。
4. 重新确认 2C 支付订单表先按月、后按周使用 `business_day` 分区；单次查询通常覆盖一周，改造后 100 万笔从约半小时恢复到约 8 分钟。
5. 重新区分三个可靠性层次：数据库条件更新抢占定时父任务；Redis 功能级锁协调手工和定时两个入口；明细状态和批次事务保证不重复与续跑。
6. 重新确认任务存在 Spring Batch 分片、心跳、停止和恢复机制；外部停止很可能调用 Spring Batch 的停止 API，并同步自研任务状态为“停止中”，具体版本和 API 仍待确认。

## 5. 总体判断

本场新增的长期价值不是继续扩张整套 JD 技术栈，而是补齐分布式事务的最小知识地图，并继续坚持事实分层：已经做过的本地批处理可靠性可以深入解释，未掌握的分布式提交协议应先澄清术语再学习，不能用项目名词替代机制回答。
