# 邱哲瀚

男 | 30 岁 | [手机号] | [邮箱] | 深圳

4 年工作经验 | 求职意向：Java 后端工程师

## 个人优势

- 4 年 Java 后端开发经验，后期长期负责云服务结算系统的交易流水处理与核销业务域，熟悉流水、订单、辅账、ERP 入账和核销对账等复杂账务链路。
- 具备百万级批处理和亿级流水表查询优化经验，曾通过批量查询与内存匹配将 100 万笔流水处理控制在 8 分钟内，并结合执行计划与联合索引将尾页查询从 120 秒超时降至稳定 3 秒以内。
- 具备复杂批处理任务的可靠性设计与生产排障经验，落地状态抢占、任务分片、中断续跑与幂等补偿，并处理过 Spring 事务边界和 MyBatis 缓存导致的 OOM 问题。
- 具备 Python 与 AI 应用开发能力，独立建设企业知识库 RAG + Agent 系统，可将检索、模型调用和受控工具执行能力与 Java 后端工程结合。

## 专业技能

- **Java / Spring：** Java、JVM 内存分析（Heap Dump）、Spring Boot、Spring MVC、MyBatis、RESTful API、IoC / AOP、事务管理、线程池、异常处理；
- **数据与缓存：** MySQL、PostgreSQL、Redis、SQL 优化、联合索引、执行计划、分区表、批量读写；
- **任务与可靠性：** MQ、异步任务、任务分片、Redis 分布式锁、数据库状态机、业务唯一标识、幂等控制、重试补偿、中断恢复；
- **AI 工程补充：** Python、FastAPI、RAG、Embedding、pgvector、Dense / Hybrid Retrieval、LangGraph、工具调用。

## 工作经历

### 荣耀终端股份有限公司

**后端开发工程师** | 2022.07-2026.03

1. 参与云服务结算系统建设，从接口和业务场景开发逐步承担交易流水处理与核销业务域的主要开发工作，支撑百万级交易流水和千万元级订单结算。
2. 负责流水集成、订单匹配、辅账生成、ERP 推送回写、未对账池和核销策略等链路的需求迭代；围绕性能、可靠性和跨系统集成推进优化，并协同业务、测试及上下游团队完成生产问题处置。

## 项目经历

### 云服务结算系统

**后端开发工程师** | 2022.07-2026.03

**技术栈：** Java、Spring Boot、MyBatis、MySQL、Redis、MQ

1. 系统承接 FFP 账单文件，完成流水批次处理、业务规则与订单匹配、辅账生成、ERP 入账及核销对账；本人后期主要负责交易流水处理与核销业务域。
2. 定位循环逐笔查询造成的 N+1 问题，将支付订单查询改为每 1000 笔批量获取并通过 Map 完成内存匹配，使流水处理达到 100 万笔 8 分钟以内的项目基线。
3. 重构对账池生成任务的可恢复执行机制，以数据库条件更新抢占任务，结合带执行标识和 6 小时 TTL 的 Redis 锁、任务分片及明细级 Checkpoint 控制处理权；将单批入池与状态更新纳入同一事务，验证服务器重启后已成功批次不重复、未完成批次可继续。
4. 独立设计并开发结算单电子盖章模块，对接盖章中心与 EDM；以请求唯一标识和 docId 关联同步申请与异步回调，通过状态抢占、有界线程池、条件更新和超时补偿支持单请求最多 20 笔批量申请，上线运行 3 个月未发生生产故障或数据错误。
5. 排查对账池生成任务 OOM，通过 Heap Dump 发现 ReconcileData 对账对象及关联数据保留内存超过 10GB，引用链指向 MyBatis 缓存；关闭大批量查询缓存并在低堆内存环境完成开关对照，修复上线后连续运行 3 个月未再发生 OOM。
6. 主动走读撤核销任务，发现 ERP 发票与收据使用独立凭证 ID 序号池而系统未区分单据类型；构造碰撞数据复现后，将识别条件修复为“ERP 凭证 ID + 接口名”，补丁上线约一个月后在真实流水重推场景中成功避免发票侧被误撤核销。
7. 针对亿级流水表分页查询，将分页明细与 COUNT(*) 拆分为独立请求，先通过联合索引查询 ID 再回表；通过 EXPLAIN 定位尾页退化为全表扫描的问题，将分区时间字段补入联合索引，使不足一页的查询由超过 120 秒超时降至稳定 3 秒以内。

### 企业知识库 RAG + Agent 系统

**独立设计与开发** | 2026.02-至今

**技术栈：** Java、Spring Boot、Python、FastAPI、PostgreSQL、pgvector、Redis、LangGraph

1. 独立完成企业知识库 RAG + Agent 系统从 0 到 1 建设，采用 Spring Boot 管理知识库、文档任务、会话、业务状态和对外接口，FastAPI 封装 Embedding 与模型调用能力。
2. 基于 PostgreSQL + pgvector 实现文档切块、Embedding、Dense / Hybrid Retrieval、RRF 融合、TopK 召回、上下文组装与引用溯源，完成企业文档问答端到端链路。
3. 为文档解析、索引构建和重嵌入任务实现异步处理、状态落库、失败重试、中断恢复与索引就绪校验；构建受工具白名单、JSON Schema、调用次数、风险分级和人工确认约束的 Agent Runtime。

## 教育经历

### 广东工业大学

**硕士 | 仪器科学与技术** | 2019-2022

1. GPA：90.8/100，硕士推免，就读期间均获得学业奖学金；
2. 研究方向为计算机视觉、双目深度估计、多特征融合与注意力机制；
3. 以第一作者完成中文核心论文。

### 广东工业大学

**本科 | 测控技术与仪器** | 2015-2019

1. GPA：91.9/100，专业排名 1/182；
2. 就读期间均获得学业奖学金；
3. 本科毕业设计方向为深度学习与计算机视觉。

## 资格证书

- 大学英语四级
