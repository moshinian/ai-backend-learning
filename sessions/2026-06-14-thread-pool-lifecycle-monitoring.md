# 2026-06-14 Java 线程池生命周期、异常与监控

## 1. 本次学习内容

1. `keepAliveTime`、非核心线程回收和 `allowCoreThreadTimeOut`
2. 核心线程懒创建和预热方法
3. `Runnable`、`Thread`、`ThreadFactory`、Worker 和 Executor 的职责
4. `execute()`、`submit()`、`Future` 和异常传播边界
5. RAG 长任务为什么不能在 Tomcat 线程调用 `future.get()`
6. 业务异常处理、任务状态表和线程级 Handler 的职责
7. `shutdown()`、`awaitTermination()`、`shutdownNow()` 和协作式中断
8. 线程池监控、阶梯压测和容量拐点
9. 数据库连接池等待、等待位置转移和 N+1 查询

## 2. 已经稳定的部分

1. 能说明默认只回收超过核心线程数的空闲线程。
2. 能说明开启核心线程超时后线程池最终可以降到 0。
3. 能判断 `keepAliveTime` 不会中断正在执行的长任务。
4. 能说明 `ThreadFactory` 创建工作线程，`execute()` 提交业务任务。
5. 能说明 `future.get()` 会阻塞调用它的 Tomcat 请求线程。
6. 能区分任务内部异常处理和线程级 Handler 兜底。
7. 能说明 `shutdown()` 处理存量任务，`shutdownNow()` 只是发出中断请求。
8. 能根据新增与完成速率计算队列填满时间。
9. 能识别增加线程后数据库连接等待恶化时应回退并定位数据库链路。
10. 能识别和批量改写基础 N+1 查询。

## 3. 本次暴露的问题

1. 最初误以为 `keepAliveTime` 默认控制核心线程。
2. 最初误以为线程池降到 0 后任务会先入队。
3. 一开始不清楚 `ThreadFactory` 在 `execute()` 内部创建 Worker 线程的作用。
4. 一开始认为未观察的 `Future` 没有保存异常。
5. 对中断标志被 `InterruptedException` 清除的机制判断错误。
6. 把 P99 耗时误当成生产与消费速率。
7. 初始认为线程数超过 CPU 核数一定不能提高 IO 密集任务吞吐。
8. 数据库连接等待升高时，仍容易直接归因于数据库 IO，需要继续建立证据链。

## 4. 当前状态

Java 线程池继续保持 `DOING`。

生命周期、异常处理、优雅关闭和监控压测已经完成第一轮；下一步需要把线程池容量、数据库连接池和下游吞吐约束整合成稳定面试表达。

## 5. 下次恢复入口

继续回答：

> RAG 入库线程池有 32 个工作线程，数据库连接池只有 20 个连接。每个任务执行时需要占用 1 个连接，单次事务平均持续 5 秒。压测时 `activeCount=32`、数据库连接 `active=20`、`pending=12`、CPU 为 35%。这 12 个线程在做什么？为什么增加线程无法提高吞吐？是否应该直接把连接池扩大到 32？

回答时按以下顺序：

1. 先定位 12 个线程的等待位置
2. 判断真正的并发上限
3. 说明为什么 CPU 低不代表还能继续加线程
4. 列出扩大连接池前需要检查的数据库证据
